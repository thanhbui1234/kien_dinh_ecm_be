const { removeBackground } = require('@imgly/background-removal-node');
const fs = require('fs');


async function main() {
  console.log("Reading test.png (actually README.md in the mock, wait!)...");
  // wait, the user's test-api.http used `./README.md` as image/png. That's not a real image!
}
main();
